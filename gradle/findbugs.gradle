apply plugin: "findbugs"

findbugs {
	toolVersion = "3.0.1"
	effort = "max"
	findbugsTest.enabled = false
}

project.tasks.withType(FindBugs) {
	group "Verification"
	description "Run Findbugs on this project."

	reports {
		xml.enabled = false
		html.enabled = true
	}
}
